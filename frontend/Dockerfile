# Ver o status
git status

# Adicionar o Dockerfile do frontend
git add frontend/Dockerfile

# Se quiser MESMO apagar o pnpm-lock.yaml agora, você pode:
# rm pnpm-lock.yaml
# git add pnpm-lock.yaml

# Commit
git commit -m "chore: add static Dockerfile for frontend"
